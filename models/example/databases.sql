{{ config(
            materialized='table',
                post_hook={
                    "sql": "create or replace table medmongers_db.maplemonk.medmongers_pharmeasy_delhi_fact_items as select \'PHARMEASY DELHI\' as marketplace, case when (order_placed_date is null or ordeR_placed_date = \'null\') then null else to_date(order_placed_date) end as order_date, case when (invoiced_date is null or invoiced_date = \'null\') then null else to_date(invoiced_date) end as invoice_date, order_id as order_id, c_order_id as reference_code, hsn_code, cast(received_qty as int) as quantity, sku, business_category as category, order_status, case when cast(return_qty as int) > 0 then 1 else 0 end as is_refund, cast(return_qty as int) as returned_quantity, cast(return_gmv as float) as returned_selling_price, case when order_status like \'%CANCELLED%\' then cast(received_gmv as float) else 0 end as cancelled_sales, case when order_status like \'%CANCELLED%\' then cast(received_qty as float) else 0 end as cancelled_quantity, seller_name, product_name, cast(received_gmv as float) as selling_price, cast(fulfilled_gmv as float) as net_sales, cast(fulfilled_qty as float) as net_quantity, supplier_city, cast(seller_order_mrp as float) * cast(cnr_qty as int) as mrp, cancellation_reason, cast(cgst as float) as cgst, cast(igst as float) as igst, cast(sgst as float) as sgst, (cast(cgst as float) + cast(igst as float) + cast(sgst as float)) as total_tax, cast(unit_net_discount as float) * cast(cnr_qty as int) as discount from medmongers_db.maplemonk.pharmeasy_mm_sales_report qualify row_number() over (partition by order_id, sku order by order_placed_date desc) = 1 ; create or replace table medmongers_db.maplemonk.medmongers_pharmeasy_blr_fact_items as select \'PHARMEASY BANGALORE\' as marketplace, case when (order_placed_date is null or ordeR_placed_date = \'null\') then null else to_date(order_placed_date) end as order_date, case when (invoiced_date is null or invoiced_date = \'null\') then null else to_date(invoiced_date) end as invoice_date, order_id as order_id, c_order_id as reference_code, hsn_code, cast(received_qty as int) as quantity, sku, business_category as category, order_status, case when cast(return_qty as int) > 0 then 1 else 0 end as is_refund, cast(return_qty as int) as returned_quantity, cast(return_gmv as float) as returned_selling_price, case when order_status like \'%CANCELLED%\' then cast(received_gmv as float) else 0 end as cancelled_sales, case when order_status like \'%CANCELLED%\' then cast(received_qty as float) else 0 end as cancelled_quantity, seller_name, product_name, cast(received_gmv as float) as selling_price, cast(fulfilled_gmv as float) as net_sales, cast(fulfilled_qty as float) as net_quantity, supplier_city, cast(seller_order_mrp as float) * cast(cnr_qty as int) as mrp, cancellation_reason, cast(cgst as float) as cgst, cast(igst as float) as igst, cast(sgst as float) as sgst, (cast(cgst as float) + cast(igst as float) + cast(sgst as float)) as total_tax, cast(unit_net_discount as float) * cast(cnr_qty as int) as discount from medmongers_db.maplemonk.pharmeasy_mm_blr_sales_report qualify row_number() over (partition by order_id, sku order by order_placed_date desc) = 1 ; create or replace table medmongers_db.maplemonk.medmongers_pharmeasy_mum_fact_items as select \'PHARMEASY MUMBAI\' as marketplace, case when (order_placed_date is null or ordeR_placed_date = \'null\') then null else to_date(order_placed_date) end as order_date, case when (invoiced_date is null or invoiced_date = \'null\') then null else to_date(invoiced_date) end as invoice_date, order_id as order_id, c_order_id as reference_code, hsn_code, cast(received_qty as int) as quantity, sku, business_category as category, order_status, case when cast(return_qty as int) > 0 then 1 else 0 end as is_refund, cast(return_qty as int) as returned_quantity, cast(return_gmv as float) as returned_selling_price, case when order_status like \'%CANCELLED%\' then cast(received_gmv as float) else 0 end as cancelled_sales, case when order_status like \'%CANCELLED%\' then cast(received_qty as float) else 0 end as cancelled_quantity, seller_name, product_name, cast(received_gmv as float) as selling_price, cast(fulfilled_gmv as float) as net_sales, cast(fulfilled_qty as float) as net_quantity, supplier_city, cast(seller_order_mrp as float) * cast(cnr_qty as int) as mrp, cancellation_reason, cast(cgst as float) as cgst, cast(igst as float) as igst, cast(sgst as float) as sgst, (cast(cgst as float) + cast(igst as float) + cast(sgst as float)) as total_tax, cast(unit_net_discount as float) * cast(cnr_qty as int) as discount from medmongers_db.maplemonk.pharmeasy_mm_mum_sales_report qualify row_number() over (partition by order_id, sku order by order_placed_date desc) = 1 ; create or replace table medmongers_db.maplemonk.medmongers_pharmeasy_consolidated as select * from medmongers_db.maplemonk.medmongers_pharmeasy_delhi_fact_items union all select * from medmongers_db.maplemonk.medmongers_pharmeasy_blr_fact_items union all select * from medmongers_db.maplemonk.medmongers_pharmeasy_mum_fact_items ;",
                    "transaction": true
                }
            ) }}
            with sample_data as (

                select * from MEDMONGERS_DB.information_schema.databases
            ),
            
            final as (
                select * from sample_data
            )
            select * from final
            