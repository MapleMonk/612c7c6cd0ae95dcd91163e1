{{ config(
            materialized='table',
                post_hook={
                    "sql": "create or replace table PEESAFE_DB.MAPLEMONK.Pee_Safe_kwikengage_campaign_report as select to_date(date,\'YYYY-MM-DD\') as Date, case when cost = \'NA\' then null else cast(replace(cost,\',\',\'\') as float) end as Cost, case when ctr = \'NA\' then null else cast(replace(replace(ctr,\',\',\'\'),\'%\',\'\') as float)/100 end as ctr, upper(name) as campaign_name, case when replace(roas,\',\',\'\') = \'NA\' then null else cast(replace(roas,\',\',\'\') as float) end as roas, cast(replace(Sales,\',\',\'\') as float) as revenue, cast(seen as integer) as seen, cast(sent as integer) as sent, cast(buyers as integer) as customers, cast(clicks as integer) as clicks, cast(orders as integer) as orders, cast(delivered as integer) as delivered, cast(Unsubscribers as integer) as unsubscribers, upper(Channel) as channel, upper(source) as source, \'CAMPAIGN INSIGHTS\' as report_type, \'Pee Safe\' as brand from PEESAFE_DB.MAPLEMONK.PEE_SAFE_KWIKENGAGE_CAMPAIGN_INSIGHTS union all select to_date(date,\'YYYY-MM-DD\') as Date, case when cost = \'NA\' then null else cast(replace(cost,\',\',\'\') as float) end as Cost, case when ctr = \'NA\' then null else cast(replace(replace(ctr,\',\',\'\'),\'%\',\'\') as float)/100 end as ctr, upper(name) as campaign_name, case when replace(roas,\',\',\'\') = \'NA\' then null else cast(replace(roas,\',\',\'\') as float) end as roas, case when Sales = \'NA\' then null else cast(replace(Sales,\',\',\'\') as float) end as revenue, cast(seen as integer) as seen, cast(sent as integer) as sent, cast(buyers as integer) as customers, cast(clicks as integer) as clicks, cast(orders as integer) as orders, cast(delivered as integer) as delivered, cast(Unsubscribers as integer) as unsubscribers, upper(Channel) as channel, null as source, \'OTHER AUTOMATIONS\' as report_type, \'Pee Safe\' as brand from PEESAFE_DB.MAPLEMONK.PEE_SAFE_KWIKENGAGE_AUTOMATION_REPORT union all select to_date(date,\'YYYY-MM-DD\') as Date, case when cost = \'NA\' then null else cast(replace(cost,\',\',\'\') as float) end as Cost, case when ctr = \'NA\' then null else cast(replace(replace(ctr,\',\',\'\'),\'%\',\'\') as float)/100 end as ctr, upper(name) as campaign_name, case when replace(roas,\',\',\'\') = \'NA\' then null else cast(replace(roas,\',\',\'\') as float) end as roas, cast(replace(Sales,\',\',\'\') as float) as revenue, cast(seen as integer) as seen, cast(sent as integer) as sent, cast(buyers as integer) as customers, cast(clicks as integer) as clicks, cast(orders as integer) as orders, cast(delivered as integer) as delivered, cast(Unsubscribers as integer) as unsubscribers, upper(Channel) as channel, upper(source) as source, \'CAMPAIGN INSIGHTS\' as report_type, \'Furr\' as brand from PEESAFE_DB.MAPLEMONK.FURR_KWIKENGAGE_CAMPAIGN_INSIGHTS union all select to_date(date,\'YYYY-MM-DD\') as Date, case when cost = \'NA\' then null else cast(replace(cost,\',\',\'\') as float) end as Cost, case when ctr = \'NA\' then null else cast(replace(replace(ctr,\',\',\'\'),\'%\',\'\') as float)/100 end as ctr, upper(name) as campaign_name, case when replace(roas,\',\',\'\') = \'NA\' then null else cast(replace(roas,\',\',\'\') as float) end as roas, case when Sales = \'NA\' then null else cast(replace(Sales,\',\',\'\') as float) end as revenue, cast(seen as integer) as seen, cast(sent as integer) as sent, cast(buyers as integer) as customers, cast(clicks as integer) as clicks, cast(orders as integer) as orders, cast(delivered as integer) as delivered, cast(Unsubscribers as integer) as unsubscribers, upper(Channel) as channel, null as source, \'OTHER AUTOMATIONS\' as report_type, \'Furr\' as brand from PEESAFE_DB.MAPLEMONK.Furr_KWIKENGAGE_AUTOMATION_REPORT ;",
                    "transaction": true
                }
            ) }}
            with sample_data as (

                select * from PEESAFE_DB.information_schema.databases
            ),
            
            final as (
                select * from sample_data
            )
            select * from final
            