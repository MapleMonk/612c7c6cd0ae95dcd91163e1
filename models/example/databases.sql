{{ config(
                        materialized='table',
                            post_hook={
                                "sql": "create or replace table xyxx_db.maplemonk.next_purchase_xyxx_intermediate as select last_purchase, next_purchase, number, list, share from( select *, row_number() over (partition by last_category,last_range order by number desc) list, number/sum(number) over (partition by last_purchase) Share from ( select b.category last_category,b.range last_range,concat(last_category,\' \',last_range) as last_purchase, a.category next_category,a.range as next_range,concat(next_category,\' \',next_range) as next_purchase, count(*) number from ( (select row_number() over (partition by customer_id order by order_id) order_number,* from (select dense_rank() over (partition by order_id order by selling_price,product_id desc) rw, * from xyxx_db.maplemonk.sales_consolidated_xyxx) where rw = 1) )a left join ( select distinct customer_id,order_number, category,range from (select row_number() over (partition by customer_id order by order_id) order_number,* from (select dense_rank() over (partition by order_id order by selling_price ,product_id desc) rw, * from xyxx_db.maplemonk.sales_consolidated_xyxx) where rw = 1) ) b on a.order_number = (b.order_number + 1) and a.customer_id = b.customer_id where b.order_number is not null and b.customer_id is not null group by 1,2,3,4,5 order by 6 desc ) ) where list in (1,2,3); create or replace table xyxx_db.maplemonk.next_purchase_xyxx as select a.last_purchase, next_purchase1, next_purchase2, next_purchase3, share1, share2, share3 from (select last_purchase , next_purchase as next_purchase1 , share as share1 from xyxx_db.maplemonk.next_purchase_xyxx_intermediate where list = 1) a left join (select last_purchase , next_purchase as next_purchase2 , share as share2 from xyxx_db.maplemonk.next_purchase_xyxx_intermediate where list = 2) b on a.last_purchase = b.last_purchase left join (select last_purchase , next_purchase as next_purchase3 , share as share3 from xyxx_db.maplemonk.next_purchase_xyxx_intermediate where list = 3) c on a.last_purchase = c.last_purchase ; create or replace table xyxx_db.maplemonk.customer_master_intermediate as select fi.customer_id_final, max(fi.name) as Name, max(fi.email) email, fi.phone, min(Acquisition_Date) Acquisition_Date , min(case when fi.order_date = Acquisition_Date then shop_name end) as Marketplace, min(case when fi.order_date = Acquisition_Date then source end) as Acquisition_Marketing_Channel, fi.acquisition_product, datediff(day,min(order_date),current_date) days_since_first_purchase, max(case when fi.order_date=fi1.max_order_date then PRODUCT_NAME_FINAL end) last_product, ifnull(sum(fi.selling_price),0) total_sales_inr, count(distinct fi.order_id) total_orders, count(distinct case when shop_name = \'Shopify_India\' then fi.order_id end ) as total_shopify_orders, count(distinct case when shop_name = \'CRED\' then fi.order_id end ) as total_CRED_orders, count(distinct fi.SALES_ORDER_ITEM_ID) total_items, max(case when fi.order_date=fi1.max_order_date then order_id end) last_order_id, datediff(day,max(fi.order_date),current_date) days_since_last_purchase, case when count(distinct order_id)=0 then null else datediff(day,min(order_date),max(order_date))/count(distinct order_id) end Avg_days_between_purchases, sum(fi.DISCOUNT) discount_inr, case when sum(fi.selling_price)=0 then null else sum(fi.DISCOUNT)/sum(fi.selling_price) end discount_percent from xyxx_db.maplemonk.SALES_CONSOLIDATED_XYXX fi left join (select customer_id_final, max(order_date) max_order_date from xyxx_db.maplemonk.SALES_CONSOLIDATED_XYXX group by customer_id_final )fi1 on fi.customer_id_final=fi1.customer_id_final where fi.customer_id_final is not null and order_date is not null group by fi.customer_id_final, fi.phone, fi.Acquisition_Product; create or replace table xyxx_db.maplemonk.customer_master_XYXX as select x.*, rcm.category, np.next_purchase1, np.next_purchase2, np.next_purchase3 from (select c.*, m.Score as M_Score,r.Score as R_Score,f.Score as F_Score, (r.Score*1/3+f.Score*1/3+m.Score*1/3) as RFM_Score from (select *, row_number () over (order by TOTAL_Sales_INR desc)/ (select count (distinct CUSTOMER_ID_FINAL) from XYXX_DB.maplemonk.Customer_Master_Intermediate) as Percentile_M, row_number() over(order by DAYS_SINCE_LAST_PURCHASE asc,CUSTOMER_ID_FINAL)/(select count(distinct CUSTOMER_ID_FINAL) from XYXX_DB.maplemonk.Customer_Master_Intermediate) as Percentile_R, case when AVG_DAYS_BETWEEN_PURCHASES=0 then 0.4 else row_number () over (partition by case when AVG_DAYS_BETWEEN_PURCHASES=0 then 0 else 1 end order by AVG_DAYS_BETWEEN_PURCHASES asc,CUSTOMER_ID_FINAL) /(select count(distinct CUSTOMER_ID_FINAL) from XYXX_DB.maplemonk.Customer_Master_Intermediate) end as Percentile_F from XYXX_DB.maplemonk.Customer_Master_Intermediate) c left join XYXX_DB.maplemonk.RFM_Score m on c.Percentile_M > m.\"Lower Limit\" and c.Percentile_M<=m.\"Upper Limit\" left join XYXX_DB.maplemonk.RFM_Score r on c.Percentile_R > r.\"Lower Limit\" and c.Percentile_R<=r.\"Upper Limit\" left join XYXX_DB.maplemonk.RFM_Score f on c.Percentile_F > f.\"Lower Limit\" and c.Percentile_F<=f.\"Upper Limit\")x left join XYXX_DB.maplemonk.RFM_CUSTOMER_CATEGORY_MAPPING rcm on x.M_Score::int>rcm.M_START::int and x.M_Score::int<=M_END::int and x.R_Score::int>rcm.R_START::int and x.R_Score::int<=R_END::int and x.F_Score::int>rcm.F_START::int and x.F_Score::int<=F_END::int left join XYXX_DB.maplemonk.next_purchase_xyxx np on np.last_purchase = x.last_product;",
                                "transaction": true
                            }
                        ) }}
                        with sample_data as (

                            select * from XYXX_DB.information_schema.databases
                        ),
                        
                        final as (
                            select * from sample_data
                        )
                        select * from final
                        