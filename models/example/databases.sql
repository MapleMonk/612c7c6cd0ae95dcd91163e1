{{ config(
            materialized='table',
                post_hook={
                    "sql": "create or replace table MAPLEMONK.KA_SHIPROCKET_FACT_ITEMS as select SR.* ,safe_cast(zt.days as int64) DELIVERY_TAT from (select case when lower(channel_name) = \'custom\' then \'WEBSITE\' else upper(channel_name) end Marketplace , replace(JSON_EXTRACT_SCALAR(A, \"$.awb\"),\'\"\',\'\') awb ,upper(SR.zone) ZONE ,upper(shipping_method) MODE ,is_return Return_Flag ,rto_reason ,delay_reason ,channel_order_id ,safe.PARSE_TIMESTAMP(\'%d %b %Y, %I:%M %p\', created_at) Created_date ,TIMESTAMP(updated_at) Updated_date ,upper(status) status ,replace(JSON_EXTRACT_SCALAR(A, \"$.courier\"),\'\"\',\'\') Courier ,replace(JSON_EXTRACT_SCALAR(A, \"$.volumetric_weight\"),\'\"\',\'\') volumetric_weight, SR.pickup_location AS PICKUP_LOCATION ,case when (picked_up_date is NULL or picked_up_date =\"\") THEN NULL ELSE PARSE_DATETIME(\'%Y-%m-%d %H:%M:%S\', picked_up_date) END AS PICKEDUP_DATE ,case when (first_out_for_delivery_date is NULL or first_out_for_delivery_date =\"\") THEN NULL ELSE PARSE_DATETIME(\'%Y-%m-%d %H:%M:%S\', first_out_for_delivery_date) end AS FIRST_OUT_FOR_DELIVERY_DATE ,case when (out_for_delivery_date is NULL or out_for_delivery_date =\"\") THEN NULL ELSE PARSE_DATETIME(\'%d-%m-%Y %H:%M:%S\', out_for_delivery_date) end AS OUT_FOR_DELIVERY_DATE ,case when (delivered_date is NULL or delivered_date =\"\") THEN NULL ELSE safe.PARSE_DATETIME(\'%d-%m-%Y %H:%M:%S\', delivered_date) end AS DELIVERED_DATE ,\'SHIPROCKET\' as Shipment_Aggregator ,upper(payment_method) payment_method ,case when replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.charged_weight_amount_rto\"),\'\"\',\'\')=\'\' then 0 else SAFE_CAST(replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.charged_weight_amount_rto\"),\'\"\',\'\') as float64) end Return_Shipping_Charges ,case when replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.cod_charges\"),\'\"\',\'\')=\'\' then 0 else (case when safe_cast(replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.charged_weight_amount_rto\"),\'\"\',\'\') as float64) >0 then 0 else safe_cast(replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.cod_charges\"),\'\"\',\'\') as float64) end) end COD_CHARGES ,case when replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.freight_charges\"),\'\"\',\'\')=\'\' then 0 else safe_cast(replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.freight_charges\"),\'\"\',\'\') as float64) - (case when replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.cod_charges\"),\'\"\',\'\')=\'\' then 0 else (case when safe_cast(replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.charged_weight_amount_rto\"),\'\"\',\'\') as float64) >0 then 0 else safe_cast(replace(JSON_EXTRACT_SCALAR(awb_data, \"$.charges.cod_charges\"),\'\"\',\'\') as float64) end) end) end Forward_Shipping_Charges from MAPLEMONK.kA_SHIPROCKET_ORDERS SR , UNNEST(SHIPMENTS) AS A qualify row_number() over (partition by replace(JSON_EXTRACT_SCALAR(A, \"$.awb\"),\'\"\',\'\') order by TIMESTAMP(updated_at) desc) =1 ) SR left join (select * from MAPLEMONK.KA_GS_ZONE_TAT_MAPPING qualify row_number() over (partition by lower(zone), lower(mode) order by 1) =1 )zt on lower(SR.zone) = lower(zt.zone) ;",
                    "transaction": true
                }
            ) }}
            with sample_data as (

                select * from maplemonk.INFORMATION_SCHEMA.TABLES
            ),
            
            final as (
                select * from sample_data
            )
            select * from final
            