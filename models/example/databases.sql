{{ config(
            materialized='table',
                post_hook={
                    "sql": "create or replace table SLEEPYCAT_DB.MAPLEMONK.SLEEPYCAT_PandL as select SALEORDERITEMCODE ,a.source as Marketing_Source ,coalesce(a.channel,p_sp.channel) as Marketing_Channel ,a.marketplace ,a.FINAL_MARKETPLACE ,coalesce(a.order_Date::date,p_sp.date) as Date ,a.order_id ,a.reference_code ,a.shop_name ,a.return_flag ,a.new_customer_flag ,a.customer_id_final ,payment_mode ,payment_gateway ,a.awb ,a.order_Status ,a.shipping_status ,a.final_shipping_status ,case when lower(order_Status) like \'%cancel%\' then upper(order_status) else upper(coalesce(final_shipping_status,order_status)) end final_status ,a.sku ,coalesce(a.sku_code,p_sp.skucode) sku_code ,a.common_sku_code ,a.product_name_final PRODUCT_NAME_FINAL ,a.product_sub_category product_sub_category ,a.product_category PRODUCT_CATEGORY ,a.quantity ,d.gst ,a.return_amount ,a.total_mrp ,a.selling_price ,a.shipping_price shipping_price ,case when lower(coalesce(final_status,\'1\')) not in (\'cancelled\',\'rto\') then d.cogs*quantity else 0 end as COGS ,a.tax tax ,a.RETURN_TAX ,div0(ifnull(p_sp.spend,0), count(1) over (partition by coalesce(a.order_Date::date,p_sp.date) ,coalesce(a.channel,p_sp.channel),coalesce(a.sku_code,p_sp.skucode) )) as Spend ,case when a.new_customer_flag = \'Repeat\' then LAG(a.order_date) IGNORE NULLS OVER (partition by a.customer_id_final ORDER BY a.order_date) end previous_date ,datediff(day,previous_date,a.order_Date) days_from_last_order ,m_c.COMMISSION_VALUE as marketing_gateway_cost_percentage ,l_c.COMMISSION_VALUE as logistic_cost_percentage ,a.selling_price*m_c.COMMISSION_VALUE AS marketing_gateway_cost ,a.selling_price*l_c.COMMISSION_VALUE AS logistic_cost ,div0(ifnull(a_c.COMMISSION_VALUE,0), count(1) over (partition by a.order_Date::date)) as admin_cost from ( select *, case when lower(marketplace) = \'amazon\' then \'AMAZON\' when lower(marketplace) = \'flipkart\' then \'FLIPKART\' when lower(marketplace) = \'woocommerce\' then \'WEBSITE\' else \'OTHERS\' END as FINAL_MARKETPLACE FROM SLEEPYCAT_DB.MAPLEMONK.SLEEPYCAT_db_sales_consolidated ) a left join ( select * from (select sku_code , try_to_date(start_date,\'DD-MON-YY\') start_Date , try_to_date(end_date,\'DD-MON-YY\') End_date , try_to_double(mrp) mrp , try_to_double(cogs) cogs , div0(try_to_double(gst),100) gst ,\'sales\' as TYPE , row_number() over (partition by sku_code, start_date, end_date order by mrp desc) rw from SLEEPYCAT_DB.MAPLEMONK.SKU_MRP_COGS ) where rw=1 ) d on lower(replace(d.sku_code,\' \',\'\')) = lower(replace(a.common_sku_code,\' \',\'\')) and to_date(a.order_date)::date >= d.start_date and to_date(a.order_date)::date <= d.end_date and lower(a.type) = lower(d.type) full outer join ( Select date ,channel ,ifnull(skucode,\'NotMapped\') as skucode ,\'sales\' as TYPE ,sum(ifnull(spend,0)) spend from sleepycat_db.maplemonk.sleepycat_db_Product_Marketing_Consolidated group by 1,2,3 )p_sp on p_sp.date = a.order_date and lower(p_sp.channel) = lower(a.channel) and lower(a.sku_code) = lower(p_sp.skucode) and lower(a.type) = lower(p_sp.type) left join (select TO_DATE(END_DATE, \'DD-Mon-YY\')END_DATE, TO_DATE(start_date, \'DD-Mon-YY\')START_DATE, MARKETPLACE, COMMISSION_TYPE, \'sales\' as TYPE, div0((replace(COMMISSION_VALUE,\'%\',\'\')::float),100) COMMISSION_VALUE from sleepycat_db.maplemonk.marketplace_commissions) m_c on a.order_Date::date >= m_c.START_DATE and a.order_Date::date <= m_c.END_DATE and lower(a.FINAL_MARKETPLACE) = lower(m_c.MARKETPLACE) and lower(a.type) = lower(m_c.type) left join (select TO_DATE(END_DATE, \'DD-Mon-YY\')END_DATE, TO_DATE(start_date, \'DD-Mon-YY\')START_DATE, MARKETPLACE, COMMISSION_TYPE, \'sales\' as TYPE, div0((replace(COMMISSION_VALUE,\'%\',\'\')::float),100) COMMISSION_VALUE from sleepycat_db.maplemonk.logistic_costs) l_c on a.order_Date::date >= l_c.START_DATE and a.order_Date::date <= l_c.END_DATE and lower(a.FINAL_MARKETPLACE) = lower(l_c.MARKETPLACE) and lower(a.type) = lower(l_c.type) left join (select TO_DATE(END_DATE, \'DD-Mon-YY\')END_DATE, TO_DATE(START_date, \'DD-Mon-YY\')START_DATE, COMMISSION_TYPE, \'sales\' as TYPE, div0(COMMISSION_VALUE,(datediff(\'day\',START_DATE,END_DATE))::int + 1) as COMMISSION_VALUE from sleepycat_db.maplemonk.other_operation_costs) a_c on a.order_Date::date >= a_c.START_DATE and a.order_Date::date <= a_c.END_DATE and lower(a.type) = lower(a_c.type) ;",
                    "transaction": true
                }
            ) }}
            with sample_data as (

                select * from SLEEPYCAT_DB.information_schema.databases
            ),
            
            final as (
                select * from sample_data
            )
            select * from final
            