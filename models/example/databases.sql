{{ config(
            materialized='table',
                post_hook={
                    "sql": "create or replace table datalake_db.kaya.dwh_gross_contribution_KAYA_new as with additional_sku_mapping as ( select sku_code sku, ifnull(replace(MRP, \',\'), 0)::float mrp, null GST, null sku_weight, from datalake_db.kaya.mst_google_sheet_new_sku_mrp_cogs qualify row_number() over( partition by lower(sku_code) order by 1 ) = 1 ), orders as ( select * from datalake_db.kaya.dwh_SHOPIFY_FACT_ITEMS ) select Shopify_customer_id_final, final_utm_channel final_utm_channel_nr, order_timestamp, order_id, ordeR_name, sku, product_name, product_category, product_list, mrp_sales, total_sales, total_discount, tax, shipping_price_without_tax, shipping_price, div0(ifnull(total_discount, 0), ifnull(mrp_sales, 0)) discount_percent, m.cancelleD_sales, m.return_sales, ifnull(m.NR, 0) NR, COGS total_COGS, 39 as logistics_cost, 21 as spm_cost, coalesce(45, total_sales * courier_charges_factor) courier_charges, coalesce(95, total_sales * supply_chain_charges_factor) supply_chain_charges, total_sales * gateway_commission_factor gateway_commission, ifnull(m.NR, 0) - ifnull(total_COGS, 0) - ifnull(courier_charges, 0) - ifnull(supply_chain_charges, 0) - ifnull(gateway_commission, 0) as GC, div0(GC, NR) GC_percent, discount_code, GC_percent * 100 GC_filter, order_timestamp::string timestamped_date from ( select order_timestamp, final_utm_channel, Shopify_customer_id_final, order_id, ordeR_name, discount_code, o.sku, o.product_name, product_category, listagg(product_name, \', \') product_list, sum(ifnull(o.total_sales, 0)) total_Sales, sum(ifnull(b.mrp * o.quantity, 0)) mrp_sales, sum( case when order_status = \'CANCELLED\' then ifnull(o.total_sales, 0) end ) cancelled_sales, sum( case when order_status <> \'CANCELLED\' and is_refund = 1 then ifnull(o.total_sales, 0) end ) return_sales, sum( ifnull(b.mrp * o.quantity, 0) - ifnull(o.total_sales, 0) ) total_discount, sum(shipping_price) shipping_price, sum(div0(ifnull(o.shipping_price, 0), 1.18)) shipping_price_without_tax, sum( case when ordeR_Status <> \'CANCELLED\' then ifnull(o.total_sales, 0) - case when is_refund = 1 then ifnull(o.total_sales, 0) else 0 end end ) net_revenue, sum( div0( case when ordeR_Status <> \'CANCELLED\' then ifnull(total_sales, 0) - ifnull( case when is_refund = 1 then ifnull(total_sales, 0) else 0 end, 0 ) end, 1.18 ) ) NR, ifnull(net_revenue, 0) - ifnull(NR, 0) tax, ifnull( sum(ifnull(b.cogs, 0) * ifnull(o.quantity, 0)), sum(ifnull(b.mrp * o.quantity, 0)) * 0.15 ) COGS, sum(ifnull(o.quantity, 0)) quantity, from orders o left join additional_sku_mapping asm on o.sku = asm.sku left join ( select * from ( select sku_code, start_Date, end_date, case when replace(replace(cogs, \',\', \'\'), \'-\', \'\') = \'\' then 0 else replace(replace(cogs, \',\', \'\'), \'-\', \'\')::float end cogs, case when replace(replace(mrp, \',\', \'\'), \'-\', \'\') = \'\' then 0 else replace(replace(mrp, \',\', \'\'), \'-\', \'\')::float end mrp, row_number() over ( partition by lower(sku_code), start_date, end_Date order by 1 ) rw from datalake_db.kaya.mst_google_sheet_new_sku_mrp_cogs ) where rw = 1 ) b on lower(o.sku) = lower(b.sku_code) and o.ordeR_timestamp::date >= b.start_Date and o.order_timestamp::date <= b.end_Date group by 1, 2, 3, 4, 5, 6, 7, 8, 9 ) m left join ( select month, replace(factor, \'%\', \'\') / 100 courier_charges_factor from datalake_db.kaya.mst_pandlassumptions where metric = \'Courier Charges\' ) n on date_trunc(month, m.order_timestamp) = n.month left join ( select month, replace(factor, \'%\', \'\') / 100 supply_chain_charges_factor from datalake_db.kaya.mst_pandlassumptions where metric = \'Supply Chain Charges\' ) o on date_trunc(month, m.order_timestamp) = o.month left join ( select month, replace(factor, \'%\', \'\') / 100 gateway_commission_factor from datalake_db.kaya.mst_pandlassumptions where metric = \'Gateway Commission\' ) p on date_trunc(month, m.order_timestamp) = p.month;",
                    "transaction": true
                }
            ) }}
            with sample_data as (

                select * from DATALAKE_DB.information_schema.databases
            ),
            
            final as (
                select * from sample_data
            )
            select * from final
            