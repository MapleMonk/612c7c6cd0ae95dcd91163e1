{{ config(
            materialized='table',
                post_hook={
                    "sql": "Create Or Replace table Maplemonk.cc_affiliate_Fact_Items as With Affiliate_Sales as ( select om.* ,aff.aff_net_name ,aff.order_commission ,order_network ,safe_divide(ifnull(selling_price,0), count(*) over(partition by order_id,brand,region)) as Calculated_Sales from ( select order_id, order_date, brand, region, sum(ifnull(selling_price,0))selling_price from `MAPLEMONK.CC_sales_consolidated` where Refund_Amount is null and order_id is not null group by 1,2,3,4 )om left join ( select order_id af_order_id,aff_net_name,order_network,order_commission from cc_comfort_db.affiliates_network_sales_track_data s left join ( select distinct aff_net_id,aff_net_name from cc_comfort_db.affiliates_network )m on cast(s.order_network as int64) = (cast(m.aff_net_id as int64)) where lower(ifnull(order_status,\'\')) in (\'approved\',\'pending\') ) aff on casT(aff.af_order_id as string) = om.order_id where af_order_id is not null ) , Affiliate_Monthly_Spends as ( select coalesce(order_date,last_day_of_month) Date, order_id, brand, region, aff_net_name, coalesce(order_network,network_id) network_id, order_commission, Calculated_Sales as selling_price, safe_divide(Calculated_Sales,sum(Calculated_Sales) over(partition by last_day(order_date),order_network)) share, ifnull(cost_value,0) * (safe_divide(Calculated_Sales,sum(Calculated_Sales) over(partition by last_day(order_date),order_network))) as Affiliate_Spend from (select * from Affiliate_Sales where order_date <= (select max(LAST_DAY(DATE(cast(year_number as int64), cast(financial_month as int64), 1))) from cc_comfort_db.amr_network_cost_data) )fi full outer join ( select network_id ,LAST_DAY(DATE(cast(year_number as int64), cast(financial_month as int64), 1)) AS last_day_of_month ,sum(ifnull(cost_value,0)) cost_value from cc_comfort_db.amr_network_cost_data group by 1,2 )ncd on last_day(fi.order_date) = ncd.last_day_of_month and fi.order_network = ncd.network_id ), final_result as ( (select Date, order_id, brand, region, aff_net_name, network_id, selling_price as selling_price_Pre, Affiliate_Spend from Affiliate_Monthly_Spends ) union all ( select coalesce(Date,generated_date) Date, order_id, coalesce(brand,af_ov_Brand) brand, coalesce(region,country) region, coalesce(aff_net_name,name) aff_net_name, coalesce(network_id,cast(id as int64)) network_id, selling_price, ifnull(order_commission,0) + ifnull(order_commission,0) * ifnull(Percenatge,0) + ifnull(safe_divide(ifnull(af.fee,0) ,count(*) over(partition by coalesce(Date,generated_date),lower(coalesce(brand,af_ov_Brand)),lower(coalesce(region,country)),coalesce(network_id,cast(id as int64)))),0) as Affiliate_Spend from ( select order_id, order_date date , brand, region, aff_net_name , case when ifnull(order_network,0) in (5,6,7) then order_commission else ifnull(order_commission,0)/ifnull(exchange_rate,1) end as order_commission , order_network as network_id, Calculated_Sales as selling_price , from( select A_S.*,exchange_rate from Affiliate_Sales A_S left join ( select order_id,exchange_rate from cc_comfort_db.order_master qualify row_number() over(partition by order_id order by ifnull(exchange_rate,0) desc) = 1 )ex on cast(A_S.order_id as string) = cast(ex.order_id as string) ) where order_date > (select max(LAST_DAY(DATE(cast(year_number as int64), cast(financial_month as int64), 1))) from cc_comfort_db.amr_network_cost_data) )fi left join ( select trim(id) as aff_id, upper(trim(brand)) ov_Brand, cast(Start_Month as date) as Start_Month, last_day(cast(End_Month as date)) as End_Month, cast(replace(Percentage,\'%\',\'\') as float64)/100 as Percenatge from `MAPLEMONK.CC_Affiliate_Override_fee` qualify row_number() over(partition by trim(id),upper(trim(brand)) order by CAST(Start_Month AS DATE) asc) = 1 )pr on fi.date >= pr.Start_Month and fi.date <= pr.End_Month and cast(fi.network_id as string) = (pr.aff_id) and lower(trim(fi.brand)) = lower(trim(pr.ov_Brand)) full outer join ( WITH DateRange AS ( SELECT TRIM(id) AS ID, UPPER(TRIM(name)) as name, UPPER(TRIM(brand)) AS brand, UPPER(TRIM(country)) AS country, CAST(Start_Month AS DATE) AS Start_Month, CAST(End_Month AS DATE) AS End_Month, CAST(REPLACE(fee, \'%\', \'\') AS FLOAT64) AS fee FROM `MAPLEMONK.CC_Affiliate_Fee` qualify row_number() over(partition by trim(id),upper(trim(brand)),UPPER(TRIM(country)) order by CAST(Start_Month AS DATE) asc) = 1 ) , ExpandedDates AS ( SELECT ID, name, brand, country, fee, Start_Month, End_Month, generated_date FROM DateRange, UNNEST(GENERATE_DATE_ARRAY(Start_Month, End_Month)) AS generated_date WHERE Start_Month IS NOT NULL AND End_Month IS NOT NULL ) SELECT ID, name, brand af_ov_Brand, country, safe_divide(fee,EXTRACT(DAY FROM DATE(last_day(generated_date)))) as fee, Start_Month, End_Month, generated_date FROM ExpandedDates where generated_date < current_date() )AF on fi.date = AF.generated_date and cast(fi.network_id as string) = (AF.id) and lower(trim(fi.brand)) = lower(trim(AF.af_ov_Brand)) and lower(trim(fi.region)) = lower(trim(AF.country)) where coalesce(Date,generated_date) > (select max(LAST_DAY(DATE(cast(year_number as int64), cast(financial_month as int64), 1))) from cc_comfort_db.amr_network_cost_data) ) ) select *,(case when lower(channel) like \'%affiliate%\' then ifnull(selling_price_pre,0) else 0 end) selling_price from final_result AFI left join ( select transactionid, customChannelGroup as final_source, customChannelGroup channel from maplemonk.CC_GA_CUSTOM_ORDER_BY_SOURCE_CONSOLIDATED qualify row_number() over (partition by replace(transactionid, \'#\',\'\') order by 1) =1 ) GA4 on replace(cast(AFI.order_id as string),\'#\',\'\') = replace(GA4.transactionid, \'#\',\'\') ;",
                    "transaction": true
                }
            ) }}
            with sample_data as (

                select * from maplemonk.INFORMATION_SCHEMA.TABLES
            ),
            
            final as (
                select * from sample_data
            )
            select * from final
            