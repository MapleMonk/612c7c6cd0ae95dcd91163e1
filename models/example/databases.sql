{{ config(
                        materialized='table',
                            post_hook={
                                "sql": "create or replace table rpsg_db.maplemonk.next_purchase_intermediate_drv as select last_product, next_product, number, list, share from( select *, row_number() over (partition by last_product order by number desc) list, number/sum(number) over (partition by last_product) Share from ( select b.productname last_product, a.productname next_product, count(*) number from ( select row_number() over (partition by customer_id_final order by order_id) order_number,* from (select dense_rank() over (partition by order_id order by selling_price,product_id desc) rw, * from rpsg_db.maplemonk.sales_consolidated_drv where lower(shop_name) like \'%shopify%\' or lower(shop_name) like \'%woocommerce%\') where rw = 1 )a left join ( select distinct customer_id_final ,order_number, productname from (select row_number() over (partition by customer_id_final order by order_id) order_number,* from (select dense_rank() over (partition by order_id order by selling_price,product_id desc) rw, * from rpsg_db.maplemonk.sales_consolidated_drv where lower(shop_name) like \'%shopify%\' or lower(shop_name) like \'%woocommerce%\') where rw = 1) ) b on a.order_number = (b.order_number + 1) and a.customer_id_final = b.customer_id_final where b.order_number is not null and b.customer_id_final is not null group by b.productname, a.productname order by 3 desc ) ) where list in (1,2,3) ; create or replace table rpsg_db.maplemonk.next_purchase_drv as select a.last_product, next_product1, next_product2, next_product3, share1, share2, share3 from (select last_product , next_product as next_product1 , share as share1 from rpsg_db.maplemonk.next_purchase_intermediate_drv where list = 1) a left join (select last_product , next_product as next_product2 , share as share2 from rpsg_db.maplemonk.next_purchase_intermediate_drv where list = 2) b on a.last_product = b.last_product left join (select last_product , next_product as next_product3 , share as share3 from rpsg_db.maplemonk.next_purchase_intermediate_drv where list = 3) c on a.last_product = c.last_product ; create or replace table RPSG_db.maplemonk.customer_master_intermediate as select fi.customer_id_final, max(fi.customer_name) as Name, max(fi.email) email, fi.phone, min(Acquisition_Date) Acquisition_Date , min(case when fi.order_date = Acquisition_Date then marketplace end) as Marketplace, min(case when fi.order_date = Acquisition_Date then channel end) as Acquisition_Marketing_Channel, fi.acquisition_product, datediff(day,min(order_date),current_date) days_since_first_purchase, max(case when fi.order_date=fi1.max_order_date then PRODUCTNAME end) last_product, ifnull(sum(fi.selling_price),0) total_sales_inr, count(distinct fi.order_id) total_orders, count(distinct case when shop_name = \'Shopify_India\' then fi.order_id end ) as total_shopify_orders, count(distinct case when shop_name = \'CRED\' then fi.order_id end ) as total_CRED_orders, count(distinct fi.line_item_id) total_items, max(case when fi.order_date=fi1.max_order_date then order_id end) last_order_id, datediff(day,max(fi.order_date),current_date) days_since_last_purchase, case when count(distinct order_id)=0 then null else datediff(day,min(order_date),max(order_date))/count(distinct order_id) end Avg_days_between_purchases, sum(fi.DISCOUNT) discount_inr, case when sum(fi.selling_price)=0 then null else sum(fi.DISCOUNT)/sum(fi.selling_price) end discount_percent from RPSG_db.maplemonk.SALES_CONSOLIDATED_DRV fi left join (select customer_id_final, max(order_date) max_order_date from RPSG_db.maplemonk.SALES_CONSOLIDATED_DRV group by customer_id_final )fi1 on fi.customer_id_final=fi1.customer_id_final where fi.customer_id_final is not null and order_date is not null group by fi.customer_id_final, fi.phone, fi.Acquisition_Product; create or replace table RPSG_db.maplemonk.customer_master_DRV as select x.*, rcm.category, next_product1, next_product2, next_product3, share1, share2, share3 from (select c.*, m.Score as M_Score,r.Score as R_Score,f.Score as F_Score, (r.Score*1/3+f.Score*1/3+m.Score*1/3) as RFM_Score from (select *, row_number () over (order by TOTAL_Sales_INR desc)/ (select count (distinct CUSTOMER_ID_FINAL) from RPSG_db.maplemonk.Customer_Master_Intermediate) as Percentile_M, row_number() over(order by DAYS_SINCE_LAST_PURCHASE asc,CUSTOMER_ID_FINAL)/(select count(distinct CUSTOMER_ID_FINAL) from RPSG_db.maplemonk.Customer_Master_Intermediate) as Percentile_R, case when AVG_DAYS_BETWEEN_PURCHASES=0 then 0.4 else row_number () over (partition by case when AVG_DAYS_BETWEEN_PURCHASES=0 then 0 else 1 end order by AVG_DAYS_BETWEEN_PURCHASES asc,CUSTOMER_ID_FINAL) /(select count(distinct CUSTOMER_ID_FINAL) from RPSG_db.maplemonk.Customer_Master_Intermediate) end as Percentile_F from RPSG_db.maplemonk.Customer_Master_Intermediate) c left join RPSG_db.maplemonk.RFM_Score m on c.Percentile_M > m.\"Lower Limit\" and c.Percentile_M<=m.\"Upper Limit\" left join RPSG_db.maplemonk.RFM_Score r on c.Percentile_R > r.\"Lower Limit\" and c.Percentile_R<=r.\"Upper Limit\" left join RPSG_db.maplemonk.RFM_Score f on c.Percentile_F > f.\"Lower Limit\" and c.Percentile_F<=f.\"Upper Limit\")x left join RPSG_db.maplemonk.RFM_CUSTOMER_CATEGORY_MAPPING rcm on x.M_Score::int>rcm.M_START::int and x.M_Score::int<=M_END::int and x.R_Score::int>rcm.R_START::int and x.R_Score::int<=R_END::int and x.F_Score::int>rcm.F_START::int and x.F_Score::int<=F_END::int left join rpsg_db.maplemonk.next_purchase_drv np on np.last_product = x.last_product ;",
                                "transaction": true
                            }
                        ) }}
                        with sample_data as (

                            select * from snitch_db.information_schema.databases
                        ),
                        
                        final as (
                            select * from sample_data
                        )
                        select * from final
                        