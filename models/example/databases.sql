{{ config(
            materialized='table',
                post_hook={
                    "sql": "create or replace table neon-poetry-482906-j7.Maplemonk.atovio_pandl as with orders as ( select cast(order_Date as date) order_Date, reference_code, sku, Marketplace, upper(channel) marketing_channel, order_status, return_type, sum(mrp_sales) mrp_sales, sum(total_cogs) total_cogs, sum(CASE WHEN LOWER(return_type) = \'return\' THEN COALESCE(total_cogs, 0)ELSE 0 END) AS Return_cogs, sum(tax) tax, sum(selling_price) selling_price, sum(mrp_sales - selling_price) as discount, sum(total_return_amount) total_return_amount from neon-poetry-482906-j7.Maplemonk.neon_poetry_482906_j7_sales_consolidated group by 1,2,3,4,5,6,7 ), marketing_spend as ( select date ,case when lower(channel) like any (\'%facebook%\', \'%google%\') then \'WEBSITE\' when lower(channel) like any (\'%amazon%\') then \'AMAZON\' when lower(channel) like \'%flipkart%\' then \'FLIPKART\' when lower(channel) like \'%myntra%\' then \'MYNTRA\' else upper(channel) end as Marketplace ,upper(channel) marketing_Channel ,sum(ifnull(spend,0)) as marketing_spend from neon-poetry-482906-j7.Maplemonk.neon_poetry_482906_j7_MARKETING_CONSOLIDATED group by 1,2,3 ), influencer_spend as ( select date_trunc(cast(date as date), month) month, sum(cast(replace(Actual_Cost____,\',\',\'\') as float64)) influencer_spend from maplemonk.gs_influencer group by 1 ), agency_spend as ( select date_trunc(cast(month as date), month) month, sum(cast(replace(Total_Price__INR_,\',\',\'\') as float64)) agency_spend from maplemonk.gs_agency group by 1 ), content_spend as ( select date_trunc(cast(month as date), month) month, sum(cast(replace(Original_Amount,\',\',\'\') as float64)) content_spend from maplemonk.gs_content_creative group by 1 ), software_spend_month as ( select date_trunc(cast(month as date), month) month, sum(cast(replace(Price__INR___excl_GST_,\',\',\'\') as float64)) software_spend from `Maplemonk.Atovio_db_gs_Software_Tools` where plan_type = \'Monthly\' group by 1 ), software_spend_Quarterly as ( select date_trunc(cast(month as date), month) month, sum(cast(replace(Price__INR___excl_GST_,\',\',\'\') as float64)) software_spend_Qu from `Maplemonk.Atovio_db_gs_Software_Tools` where plan_type = \'Quarterly\' group by 1 ), software_spend_yearly as ( select date_trunc(cast(month as date), month) month, sum(cast(replace(Price__INR___excl_GST_,\',\',\'\') as float64)) software_spend_yr from `Maplemonk.Atovio_db_gs_Software_Tools` where plan_type = \'Annually\' group by 1 ), Website_Growth_spend as ( select CAST(FORMAT_DATE(\'%Y-%m-%d\', PARSE_DATE(\'%b %y\', month)) as Date) month, sum(cast(replace(Price__INR_,\',\',\'\') as float64)) Website_growth from `Maplemonk.gs_Website_Growth` group by 1 ), porter_logistic_cost_month as ( select DATE_TRUNC(DATE(order_completion_time), month) AS month, sum(cast(taxable_amount as float64)) Porter_logistic_cost from maplemonk.atovio_db_porter_charges group by 1 ), Amazon_commission as ( SELECT date_trunc(cast(dataEndTime as date),month) month, SUM(CASE WHEN LOWER(amount_description) LIKE \'%commission%\' THEN SAFE_CAST(amount AS FLOAT64) ELSE 0 END) AS commission FROM `Maplemonk.atovio_sc_GET_V2_SETTLEMENT_REPORT_DATA_FLAT_FILE_V2` WHERE marketplace_name = \'Amazon.in\' AND transaction_type = \'Order\' GROUP BY 1 ), Shiprocket_Orders as ( select ID, SPLIT(channel_order_id, \'_\')[OFFSET(0)] order_name, channel_order_id, zone, UPPER(status) AS status, SAFE_CAST(NULLIF(JSON_VALUE(awb_data, \'$.charges.freight_charges\'), \'\') AS FLOAT64) freight_charge, SAFE_CAST(NULLIF(JSON_VALUE(awb_data, \'$.charges.cod_charges\'), \'\') AS FLOAT64) cod_charge, GREATEST( COALESCE(SAFE_CAST( NULLIF(JSON_VALUE(awb_data, \'$.charges.applied_weight_amount_rto\'), \'\')AS FLOAT64),0), COALESCE(SAFE_CAST(NULLIF(JSON_VALUE(awb_data, \'$.charges.charged_weight_amount_rto\'), \'\') AS FLOAT64),0)) AS weight_amount, REPLACE(cast(JSON_EXTRACT_SCALAR(A, \'$.courier\') as string), \'\"\', \'\') AS courier, REPLACE(cast(JSON_EXTRACT_SCALAR(P, \'$.channel_sku\') as string), \'\"\', \'\') AS channel_sku, coalesce(REPLACE(cast(JSON_EXTRACT_SCALAR(A, \'$.rto_awb\') as string), \'\"\', \'\'), REPLACE(cast(JSON_EXTRACT_SCALAR(A, \'$.return_awb\') as string), \'\"\', \'\')) AS Return_awb, FROM Maplemonk.shiprocket_atovio_orders left join UNNEST(SHIPMENTS) AS A left join UNNEST(products) AS P Qualify row_number() over(partition by SPLIT(channel_order_id, \'_\')[OFFSET(0)] , REPLACE(cast(JSON_EXTRACT_SCALAR(P, \'$.channel_sku\') as string), \'\"\', \'\') order by updated_at desc) = 1 ) select o.*, freight_charge, cod_charge, weight_amount, status shiprocket_status, marketing_spend, safe_divide(influencer_spend, count(*) over (partition by date_trunc(o.ordeR_date, month), o.return_type order by 1)) influencer_Spend, safe_divide(agency_Spend, count(*) over (partition by date_trunc(o.ordeR_date, month), o.return_type order by 1)) agency_Spend, safe_divide(content_spend, count(*) over (partition by date_trunc(o.ordeR_date, month), o.return_type order by 1)) content_spend, safe_divide(software_spend, count(*) over (partition by date_trunc(o.ordeR_date, month), o.return_type order by 1)) software_spend, safe_divide(software_spend_Qu, count(*) over (partition by date_trunc(o.ordeR_date, month), o.return_type order by 1)) software_spend_Qu, safe_divide(software_spend_Yr, count(*) over (partition by date_trunc(o.ordeR_date, month), o.return_type order by 1)) software_spend_Yr, safe_divide(Website_Growth, count(*) over (partition by date_trunc(o.ordeR_date, month), o.return_type order by 1)) Website_Growth, safe_divide(Porter_logistic_cost, count(*) over (partition by date_trunc(o.ordeR_date, month), o.return_type order by 1)) Porter_logistic_cost, safe_divide(commission, count(*) over (partition by date_trunc(o.ordeR_date, month), o.return_type order by 1)) commission from orders o full outer join marketing_spend s on date(o.order_Date) = date(s.date) and lower(o.marketing_channel)=lower(s.marketing_channel) and lower(o.Marketplace)=lower(s.Marketplace) and o.return_type is null left join influencer_spend ins on date_trunc(o.ordeR_date, month) = ins.month and o.return_type is null left join agency_spend ags on date_trunc(o.ordeR_date, month) = ags.month and o.return_type is null left join content_spend cs on date_trunc(o.ordeR_date, month) = cs.month and o.return_type is null left join software_spend_month spm on date_trunc(o.ordeR_date, month) = spm.month and o.return_type is null left join software_spend_Quarterly spq on date_trunc(o.ordeR_date, month) = spq.month and o.return_type is null left join software_spend_yearly spy on date_trunc(o.ordeR_date, month) = spy.month and o.return_type is null left join Website_Growth_spend wgs on date_trunc(o.ordeR_date, month) = wgs.month and o.return_type is null left join porter_logistic_cost_month ps on date_trunc(o.ordeR_date, month) = ps.month and o.return_type is null left join Amazon_commission ac on date_trunc(o.ordeR_date, month) = ac.month and o.return_type is null LEFT JOIN shiprocket_orders sr ON TRIM(REPLACE(REPLACE(LOWER(o.reference_code), \'#\', \'\'), \"\'\", \"\")) = TRIM(REPLACE(REPLACE(LOWER(sr.order_name), \'#\', \'\'), \"\'\", \"\")) AND TRIM(REPLACE(REPLACE(LOWER(o.sku), \'#\', \'\'), \"\'\", \"\")) = TRIM(REPLACE(REPLACE(LOWER(sr.channel_sku), \'#\', \'\'), \"\'\", \"\"));",
                    "transaction": true
                }
            ) }}
            with sample_data as (

                select * from maplemonk.INFORMATION_SCHEMA.TABLES
            ),
            
            final as (
                select * from sample_data
            )
            select * from final
            