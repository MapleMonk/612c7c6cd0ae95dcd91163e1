{{ config(
            materialized='table',
                post_hook={
                    "sql": "create or replace table snitch_db.maplemonk.web_app_conversion_snitch as with web2_sessions as ( select to_date(date,\'YYYYMMDD\') as date, sum(eventcount) as web2_sessions from snitch_db.maplemonk.products_funnel_metrics where eventname = \'session_start\' group by 1 ), web2_orders as ( select order_timestamp::date as date, count(distinct order_name) as web2_orders from snitch_db.maplemonk.fact_items_snitch where tags like \'%Snitch_Web%\' group by 1 ), web2_conversion as ( select a.*, b.web2_orders, div0(b.web2_orders,a.web2_sessions) as web2_conversion from web2_sessions a left join web2_orders b on a.date = b.date ), android2_sessions as ( select to_date(date,\'YYYYMMDD\') date, sum(eventcount) app_Android_sessions from snitch_db.maplemonk.search_app2_0_eventname_eventcount where eventname = \'session_start\' and lower(operatingsystem) = \'android\' group by 1 ), android2_orders as ( select order_timestamp::date date, count(distinct order_id) app_Android_orders from snitch_db.maplemonk.fact_items_snitch where webshopney = \'Snitch_IOS\' and tags like \'%Snitch_Android%\' group by 1 ), android_conversion as ( select a.*, b.app_Android_orders, div0(app_android_orders,app_android_sessions) as android2_conversion from android2_sessions a left join android2_orders b on a.date = b.date ), ios2_sessions as ( select to_date(date,\'YYYYMMDD\') date, sum(eventcount) app_IOS_sessions from snitch_db.maplemonk.search_app2_0_eventname_eventcount where eventname = \'session_start\' and lower(operatingsystem) = \'ios\' group by 1 ), ios2_orders as ( select order_timestamp::date date, count(distinct order_id) app_IOS_orders from snitch_db.maplemonk.fact_items_snitch where webshopney = \'Snitch_IOS\' and tags like \'%Snitch_IOS%\' group by 1 ), ios2_conversion as ( select a.*, b.app_IOS_orders, div0(b.app_IOS_orders,a.app_IOS_sessions) as ios2_conversion from ios2_sessions a left join ios2_orders b on a.date = b.date ), new_conversion as ( select a.*, b.app_IOS_sessions, b.app_IOS_orders, b.ios2_conversion, c.web2_orders, c.web2_sessions, c.web2_conversion from android_conversion a left join ios2_conversion b on a.date = b.date left join web2_conversion c on a.date = c.date ), web_sessions as ( select date, sum(web_sessions) as web_sessions from (select to_date(date, \'YYYYMMDD\') date, sum(sessions) as web_sessions from snitch_db.maplemonk.web_sessions_date group by 1 union all select to_date(date, \'YYYYMMDD\') date, sum(sessions) as web_sessions from snitch_db.maplemonk.ga4_snitch_mobile_app_sessions_by_date group by 1 ) group by 1 ), app_orders as ( select order_timestamp::date date, count(distinct order_id) Appbrew_orders from snitch_db.maplemonk.fact_items_snitch where webshopney = \'Appbrew\' group by 1 ), app_sessions as ( select to_date(date, \'YYYYMMDD\') date, sum(sessions) appbrew_sessions from snitch_db.maplemonk.sessions_date group by 1 ), web_orders as ( select order_timestamp::date date, count(distinct order_id) web_orders from snitch_db.maplemonk.fact_items_snitch where webshopney in (\'Web\',\'Shopney\') group by 1 ), web_conversion as ( select a.*, b.web_orders, div0(b.web_orders,web_sessions) as web_conversion from web_sessions a left join web_orders b on a.date = b.date ), app_conversion as ( select a.*, b.Appbrew_orders, div0(b.Appbrew_orders,a.appbrew_sessions) as app_conversion from app_sessions a left join app_orders b on a.date = b.date ), old_conversion as ( select a.*, b.Appbrew_orders, b.appbrew_sessions, b.app_conversion from web_conversion a left join app_conversion b on a.date = b.date ) select a.date, app_android_sessions as app_Android_sessions, app_android_orders as app_Android_orders, android2_conversion as app_Android_conversion, app_ios_sessions as app_IOS_sessions, app_ios_orders as app_IOS_orders , ios2_conversion as app_IOS_conversion, web2_orders as web2_orders, web2_sessions as web2_sessions, web2_conversion as web2_conversion, web_sessions as web_sessions, web_orders as web_orders, web_conversion as web_conversion, appbrew_orders as appbrew_orders, appbrew_sessions as appbrew_sessions, app_conversion as appbrew_conversion, ifnull(web_sessions,0) + ifnull(appbrew_sessions,0) +ifnull(app_ios_sessions,0) + ifnull(app_android_sessions,0) + ifnull(web2_sessions,0) as total_sessions, ifnull(web_orders,0) + ifnull(appbrew_orders,0) +ifnull(app_ios_orders,0) + ifnull(app_android_orders,0) + ifnull(web2_orders,0) as total_orders from new_conversion a left join old_conversion b on a.date = b.date",
                    "transaction": true
                }
            ) }}
            with sample_data as (

                select * from snitch_db.information_schema.databases
            ),
            
            final as (
                select * from sample_data
            )
            select * from final
            