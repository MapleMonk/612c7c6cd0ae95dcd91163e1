{{ config(
                        materialized='table',
                            post_hook={
                                "sql": "create or replace table vahdam_db.maplemonk.AMAZON_US_INVENTORY_SALES_SPEND_VAHDAM as With Amazonads as (select date ,asin_new ,productname ,sum(ifnull(sales_usd,0)) as Total_sales ,sum(ifnull(quantity,0)) as Units ,sum(ifnull(spend,0)) as Total_spend ,sum(sum(ifnull(quantity,0))) over (partition by ASIN_NEW order by date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) as last_3days_units ,sum(sum(ifnull(quantity,0))) over (partition by ASIN_NEW order by date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) as last_7days_units ,sum(sum(ifnull(quantity,0))) over (partition by ASIN_NEW order by date ROWS BETWEEN 29 PRECEDING AND CURRENT ROW) as last_30days_units ,sum(sum(ifnull(spend,0))) over (partition by ASIN_NEW order by date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) as last_3days_spend ,sum(sum(ifnull(spend,0))) over (partition by ASIN_NEW order by date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) as last_7days_spend ,sum(sum(ifnull(sales_usd,0))) over (partition by ASIN_NEW order by date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) as last_3days_sales ,sum(sum(ifnull(sales_usd,0))) over (partition by ASIN_NEW order by date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) as last_7days_sales from vahdam_db.maplemonk.amazonads_na_marketing group by 1,2,3 order by date desc) , Inventory as (select ASIN ,\"Product Name\" as Product_name ,sum(ifnull(inbound,0)) as Inbound_inventory ,sum(ifnull(shipped,0)) as Shipped_inventory ,sum(ifnull(working,0)) as Working_inventory ,sum(ifnull(receiving,0)) as Receiving_inventory ,sum(ifnull(Available,0)) as Available_inventory ,sum(ifnull(\"FC transfer\",0)) as FC_Transfer_inventory ,sum(ifnull(\"FC Processing\",0)) as FC_Processing_inventory from vahdam_db.maplemonk.casp_usa_get_restock_inventory_recommendations_report group by 1,2) select date ,coalesce(a.asin_new,I.ASIN) as ASIN ,coalesce(A.productname,I.Product_name) as Product_name ,A.Total_sales as Total_sales ,A.Units as Units ,A.Total_spend as Total_spend ,A.last_3days_units as last_3days_units ,A.last_7days_units as last_7days_units ,A.last_30days_units as last_30days_units ,A.last_3days_spend as last_3days_spend ,A.last_7days_spend as last_7days_spend ,A.last_3days_sales as last_3days_sales ,A.last_7days_sales as last_7days_sales ,I.Inbound_inventory as Current_Inbound_inventory ,I.Shipped_inventory as Current_Shipped_inventory ,I.Working_inventory as Current_Working_inventory ,I.Receiving_inventory as Current_Receiving_inventory ,I.Available_inventory as Current_Available_inventory ,I.FC_Transfer_inventory as Current_FC_Transfer_inventory ,I.FC_Processing_inventory as Current_FC_Processing_inventory From AMAZONADS A left join Inventory I on a.asin_new = I.ASIN order by date desc",
                                "transaction": true
                            }
                        ) }}
                        with sample_data as (

                            select * from VAHDAM_DB.information_schema.databases
                        ),
                        
                        final as (
                            select * from sample_data
                        )
                        select * from final
                        